import 'package:flutter/material.dart';

class AssetDetailWidgt extends StatelessWidget {
  const AssetDetailWidgt({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Table(
        children: [
          TableRow(children: [
            
          ]),
          TableRow(children: []),
          TableRow(children: [])
        ],
      ),
    );
  }
}
